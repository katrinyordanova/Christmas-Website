{% extends 'base.html.twig' %}

{% block body %}
    {{ user.firstName }}
    {{ user.lastName }}
    {{ user.email }}
    {{ user.address }}
    {{ user.number }}

    <a href="{{ path('user_edit', {'id':user.id}) }}" class="btn pull-right btn-success btn-xs">edit &raquo;</a>
    <a href="{{ path('user_delete', {'id':user.id}) }}" class="btn pull-right btn-danger btn-xs" >delete &raquo;</a>
    <a href="{{ path('homepage') }}" class="btn pull-right btn-default btn-xs">back &raquo;</a>
{% endblock %}